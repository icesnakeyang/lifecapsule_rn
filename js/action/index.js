import {onThemeChange} from "./theme";
import {createBlankToken, getLocalToken, loginUser} from "./user";

export default {
    onThemeChange,
    getLocalToken,
    createBlankToken,
    loginUser,
}